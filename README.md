# RAG_livres

Side project to explore the RAG features, for a group of books (epub files) on a given theme (cooking/recipe)

## Environnement
- Python 3.13 + librairies as listed in [Requirements.txt]
- the embedding is done with Mistral and requires an MISTRAL_API_KEY (free)

